# Item Tracker

This is a proof-of-concept/demo agent which will use an LLM to translate a user's question about the location of their item into a database query into a SQLite database containing a list of items and their locations.
